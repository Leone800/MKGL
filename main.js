const app = document.querySelector(".adicionais")
const add = adicionais.querySelector(".add")
const addInput = adicionais.querySelector(".busca")
const confirmar = adicionais.querySelector(".confirm")

const url = {id:"https, socket, http", image:""}
const div = document.createElement("adicionais")

url.id = crypto.randomUUID()
url.image = addInput.value()

function confirm(){
  async 
}
